/**
 * A module that provides wrappers for Minecraft's internal
 * named binary tags and conversions between the wrapped version
 * and the usable version.
 *
 * @since 1.0
 */
package dev.hawu.plugins.api.nbt;
